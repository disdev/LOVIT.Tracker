@page
@model LOVIT.Tracker.Pages.Admin.Monitors.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Monitor[0].Checkpoint)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Monitor[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Monitor[0].PhoneNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Monitor[0].Active)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Monitor) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Checkpoint.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Active)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
